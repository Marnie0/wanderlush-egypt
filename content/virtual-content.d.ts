/** Emitted by `scripts/vite-content-split.ts`: the Arabic content strings, by file and position. */
declare module "virtual:content-ar" {
  const table: Record<string, (string | string[])[]>;
  export default table;
}
